{
  "name": "users-list-server",
  "version": "1.0.0",
  "description": "Users List Application with Node.js and SQLite",
  "main": "dist/index.js",
  "scripts": {
    "start": "node dist/index.js",
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "tsc",
    "test": "jest"
  },
  "engines": {
    "node": ">=14.0.0"
  }
} 